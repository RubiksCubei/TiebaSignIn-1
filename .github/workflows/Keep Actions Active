# .github/workflows/keep-alive.yml
name: 🛡️ Keep Actions Active

on:
  # 每月 1 日 0 点执行一次；也可改成每天或每周
  schedule:
    - cron: '0 0 1 * *'

jobs:
  touch:
    runs-on: ubuntu-latest
    steps:
      - name: 打个卡，防止仓库过期
        run: echo "🤖 Keeping GitHub Actions alive $(date)"
